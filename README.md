# Nextcloud with docker and Nginx reverse proxy
 In this repository explain how to install Nextcloud with docker and Nginx reverse proxy in order to access to home data safely and build yor own cloud
